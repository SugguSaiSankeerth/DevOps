version: '3.3'

services:
   db:
     image: test_projectmysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: 5555
       MYSQL_DATABASE: spectrumdata
       MYSQL_USER: root
       
   webapi:
     depends_on:
       - db
     image: test_project 
     ports:
       - "8085:8080"
     restart: always